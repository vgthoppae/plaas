pipeline {
  agent any

  def onboardedApps = readYaml file: 'app-metadata/onboard-app-params.yml'
  
  echo "${onboardedApps}"

  parameters {
    booleanParam(defaultValue: true, description: '', name: 'userFlag')
  }

  stages {
    stage("foo") {
      steps {
          echo "flag: ${params.userFlag}"
      }
    }
  }
}